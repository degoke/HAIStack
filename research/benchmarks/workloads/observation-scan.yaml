name: observation-scan
description: List Observation ids from the in-memory store then read each resource (scan-read, not FHIR _search).
operations:
  - type: scan-read
    resourceType: Observation
    count: 40
